apiVersion: v1
kind: Service
metadata:
  name: {{ .ServiceName }}
  namespace: {{ .Namespace }}
  labels:
    app: {{ .ServiceName }}
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: {{ .Port }}
    protocol: TCP
    name: http
  selector:
    app: {{ .ServiceName }}