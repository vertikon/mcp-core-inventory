# {{ .ServiceName }} Dashboard

Dashboard de monitoramento em tempo real para acompanhar a execução do {{ .ServiceName }}.

## 🚀 Tecnologias

- **React 18** com TypeScript
- **Tailwind CSS** para estilização
- **Vite** como build tool
- **RemixIcon** para ícones
- **Axios** para requisições HTTP

## 📦 Instalação

```bash
npm install
```

## 🛠️ Desenvolvimento

```bash
npm run dev
```

O dashboard estará disponível em `http://localhost:5173` (porta padrão do Vite)

Você pode configurar a porta usando variáveis de ambiente:
- `VITE_PORT` - Porta do servidor de desenvolvimento (padrão: 5173)
- `VITE_HOST` - Host do servidor (padrão: localhost)
- `VITE_API_URL` - URL da API backend (padrão: http://localhost:8080)

## 🏗️ Build

```bash
npm run build
```

## 📁 Estrutura do Projeto

```
src/
├── components/
│   ├── charts/          # Componentes de gráficos
│   ├── layouts/         # Componentes de layout
│   ├── sections/        # Seções do dashboard
│   └── ui/              # Componentes UI reutilizáveis
├── hooks/               # Custom hooks
├── types/               # Definições TypeScript
├── App.tsx             # Componente principal
├── main.tsx            # Entry point
└── index.css           # Estilos globais
```

## 🔌 Integração com API

O dashboard espera uma API REST em `/api/v1` com os seguintes endpoints:

- `GET /api/v1/metrics` - Métricas do sistema
- `GET /api/v1/components/status` - Status dos componentes
- `GET /api/v1/components/:name/details` - Detalhes de um componente
- `GET /api/v1/alerts` - Alertas do sistema
- `GET /api/v1/cache/stats` - Estatísticas de cache

Se a API não estiver disponível, o dashboard usa dados mockados para desenvolvimento.

## 🎨 Funcionalidades

- ✅ Monitoramento em tempo real (atualização a cada 5s)
- ✅ Métricas principais (Throughput, HTTP P95, Cache Hit Ratio, etc)
- ✅ Status dos componentes do sistema
- ✅ Alertas do sistema
- ✅ Gráficos de performance
- ✅ Detalhes dos componentes em abas
- ✅ Controles rápidos e ações do sistema
- ✅ Layout responsivo

## 📝 Licença

Este projeto faz parte do template MCP-HULK.

