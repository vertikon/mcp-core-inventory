# {{ .ServiceName }} · Template Base

Use esta estrutura quando precisar de um esqueleto neutro que respeita Clean Architecture.

## Conteúdo
- `structure.yaml.tmpl`: descreve diretórios obrigatórios
- `README.md`: descrição inicial
- `configs/config.yaml`: parâmetros mínimos do serviço

Preencha `{{ "{{ .Description }}" }}` e `{{ "{{ .Version }}" }}` antes de gerar.
