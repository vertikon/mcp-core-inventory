package usecases

import (
	"context"
	
	"github.com/{{ .ModulePath }}/internal/domain"
)

// {{ .EntityName }}UseCase defines the business logic for {{ .EntityName }}
type {{ .EntityName }}UseCase struct {
	repo domain.{{ .EntityName }}Repository
	logger *zap.Logger
}

// New{{ .EntityName }}UseCase creates a new {{ .EntityName }}UseCase
func New{{ .EntityName }}UseCase(repo domain.{{ .EntityName }}Repository) *{{ .EntityName }}UseCase {
	return &{{ .EntityName }}UseCase{
		repo: repo,
		logger: logger.Get(),
	}
}

// Create creates a new {{ .EntityName }}
func (uc *{{ .EntityName }}UseCase) Create(ctx context.Context, entity *domain.{{ .EntityName }}) error {
	// Add business logic here
	return uc.repo.Create(ctx, entity)
}

// GetByID retrieves a {{ .EntityName }} by ID
func (uc *{{ .EntityName }}UseCase) GetByID(ctx context.Context, id string) (*domain.{{ .EntityName }}, error) {
	return uc.repo.GetByID(ctx, id)
}

// Update updates an existing {{ .EntityName }}
func (uc *{{ .EntityName }}UseCase) Update(ctx context.Context, entity *domain.{{ .EntityName }}) error {
	// Add business logic here
	return uc.repo.Update(ctx, entity)
}

// Delete deletes a {{ .EntityName }} by ID
func (uc *{{ .EntityName }}UseCase) Delete(ctx context.Context, id string) error {
	return uc.repo.Delete(ctx, id)
}

// List retrieves a list of {{ .EntityName }} entities
func (uc *{{ .EntityName }}UseCase) List(ctx context.Context, limit, offset int) ([]*domain.{{ .EntityName }}, error) {
	return uc.repo.List(ctx, limit, offset)
}